[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -w 2 -k gevent -b 0.0.0.0:$PORT app:app"

# Paquetes del sistema necesarios para compilar mysqlclient
[build.nixpacks]
aptPackages = ["pkg-config", "libmariadb-dev", "build-essential", "python3-dev"]
